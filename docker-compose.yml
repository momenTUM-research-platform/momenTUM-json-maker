version: "3"
volumes:
  surveys:
    driver: local
services:
  web:
    build: ./frontend
    ports:
      - "80:3000"
    container_name: "web"
  api:
    build: ./server
    ports:
      - "3000:3000"
    container_name: "api"
    volumes:
      - surveys:/usr/src/app/surveys
